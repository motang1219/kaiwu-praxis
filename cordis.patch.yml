# kaiwu-praxis 员工端补丁：把工具、7 名员工预设和终端代理挂进 host 组合。
# 预设随包放在 presets/，由 host 插件播种到 $DSH_HOME/.agent-presets（用户根）。
# 客户端「数字员工广场」UI 在 lib/client.js（dsh.client 行，见 package.json）。
- insert:
    - id: kaiwu-praxis
      name: 'kaiwu-praxis'
