# 家里电脑前置准备结果

## 结论

- 本机便携包校验、版本核对、干净状态检查、3080 启动和员工端页面检查：**通过**。
- Tailscale 已安装并在线；公司电脑节点 `pong`、3099 TCP 和健康接口：**通过**。
- 除用户已授权的 Tailscale 安装外，本次未安装其他软件；未修改 Windows 防火墙，未启用端口映射、DMZ、Tailscale Funnel 或其他公网暴露。
- 用户已自行完成模型 API 配置；本次验收未读取 API Key 输入框或模型配置文件。
- 第二轮跨设备接入：**员工端B已连接并完成终端注册**。

## 一、便携包校验

- 包名：`kaiwu-praxis-portable-v0.5.0.zip`
- 实际 SHA-256：`3EFC299E62FC033ADA214336683423DFFF1619E3BFEA3D5B2851CC63983DE16A`
- 预期 SHA-256：`3EFC299E62FC033ADA214336683423DFFF1619E3BFEA3D5B2851CC63983DE16A`
- 结果：完全一致，通过。
- 解压父目录：`D:\kaiwu-round2-home`
- 实际便携包根目录：`D:\kaiwu-round2-home\kaiwu-praxis-portable`
- 根目录直接包含：`app/`、`home/`、`runtime/`、`start.bat`。
- 解压前预检：55,614 个 ZIP 条目；未发现绝对路径或 `..` 越界项；未发现企业端插件文件名。

对应截图：`A-便携包哈希校验.png`

## 二、插件组成与首次启动前状态

`home\profiles\web\package.json` 中锁定版本如下：

| 插件 | 版本或来源 |
| --- | --- |
| `kaiwu-praxis` | `git+https://github.com/motang1219/kaiwu-praxis.git#v0.5.0` |
| `@nanmicoder/dsh-agent-teams` | `0.1.14` |
| `@vectorize-io/hindsight-coding-agents` | `0.4.3` |
| `dsh-better-sidebar` | `0.17.1` |

- 未安装 `kaiwu-praxis-enterprise`。仓内仅在员工端 README 的说明文字中提到该名称，不构成插件安装。
- 首次启动前，`home` 顶层只有 `profiles`。
- 未运行 `plugin add`、`npm install` 或 `pnpm install`。

## 三、员工端本机启动

- 启动前 3080 监听检查：无占用。
- 实际监听地址：`127.0.0.1:3080`。
- 实际进程：便携包自带 `runtime\node\node.exe`。
- HTTP 检查：`http://127.0.0.1:3080/` 返回 200。
- 页面检查：
  - “数字员工广场”可打开。
  - 共 7 名数字员工。
  - 包含 `kaiwu-brand-auditor`（品牌健康诊断专家）和 `kaiwu-data-tracker`（经营数据分析员）。
  - 前置检查阶段，“员工设置 → 企业连接”可打开，连接状态为“未注册”。
  - 未发现可操作的“企业管理”入口；左侧只有“企业管理”分组标题和“企业连接”按钮。
  - 前置检查阶段，企业注册码与终端名称均未填写，未点击“保存并连接”。

对应截图：`B-员工端本机启动.png`。截图中的自动生成终端标识已遮盖。

## 四、Tailscale 与公司中枢连通

- 已通过 Windows 软件源安装 Tailscale `1.102.3`。
- 程序路径：`C:\Program Files\Tailscale\tailscale.exe`。
- Authenticode 签名状态：`Valid`；签名者为 `Tailscale Inc.`。
- Windows 服务状态：`Running`，启动类型为 `Automatic`。
- 公司电脑 Tailscale IPv4：已收到；报告中遮盖为 `100.71.*.*`。
- 家里电脑 Tailscale 在线状态：在线，`BackendState` 为 `Running`；本机 Tailscale IPv4 在报告中遮盖为 `100.121.*.*`。
- 公司电脑节点状态：`Online: True`、`Active: True`、系统为 Windows。
- `tailscale ping`：收到 `pong`，经 `DERP(lax)` 中继，约 803 ms；仍提示 `direct connection not established`，但不影响本次 TCP/HTTP 实际连通。
- 3099 TCP：`TcpTestSucceeded : True`。
- `http://<COMPANY_TS_IP>:3099/api/health`：第二轮接入前使用 `-NoProxy` 复核通过，返回 `ok: true`、`version: 0.2.1`、企业名“开物第二轮跨设备测试企业”。
- 首次失败与复测说明：公司电脑重启中枢并修复监听前，3099 TCP 和健康接口曾超时；修复后重新执行，两项均通过。
- 判断：跨设备前置连通已通过。当前连接经 DERP 中继而非直连，性能可能较低，但不阻塞第二轮测试前置验收。

对应截图：`C-中枢前置连通.png`，记录最终复测通过结果。

## 五、第二轮跨设备接入结果

- 接入日期：2026-09-08。
- 员工端实际端口：`127.0.0.1:3080`，HTTP 状态 200。
- 企业中枢地址：报告中遮盖为 `http://100.71.*.*:3099`。
- 终端名称：`员工端B`。
- 页面最终状态：`已连接`。
- 企业名称：`开物第二轮跨设备测试企业`。
- 页面最后连接时间：`2026/9/8 10:31:51`。
- 企业注册码输入框：连接成功后已自动清空；注册码未写入报告或截图。
- 终端 ID：未写入报告，截图中已遮盖。
- 管理员令牌：未索取、未读取、未填写。

对应截图：`03-员工端B已连接.png`。

## 六、停止边界

- 员工端保持运行和“已连接”状态。
- 本次在完成单终端接入后停止，未进行批量配置、批量下发或其他正式测试。
